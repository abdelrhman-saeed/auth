Auth is a simple php package the adds authentication functionality
to your app, it supports authenticating with jwts and string based tokens for api
also supports authenticating with sessions

































